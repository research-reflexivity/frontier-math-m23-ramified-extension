# GitHub Linguist does not yet support PARI/GP.  Treat GP scripts as neutral
# text instead of misclassifying them as Gnuplot, and omit them from language
# statistics until PARI/GP is added upstream.
*.gp linguist-language=Text -linguist-detectable
